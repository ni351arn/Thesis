\begin{tikzpicture}[
  font=\sffamily,
  box/.style={draw, rounded corners=2pt, minimum width=3.2cm, minimum height=1.1cm, align=center},
  bigbox/.style={box, minimum width=4.2cm},
  arrow/.style={-Latex, line width=0.9pt},
  note/.style={font=\scriptsize, align=center},
  node distance=12mm and 18mm
]
 
% --- Nodes (Layout ähnlich deiner Skizze) ---
\node[box] (gitops) at (-4.8, 3.6) {GitOps\\Repo};
 
\node[box] (runbooks) at (0, 3.6) {Runbooks};
\node[note] at (0, 4.55) {Dort halten wir\\Fehler fest und\\beschreiben die\\L\"osungsschritte};
 
\node[box] (k8s) at (6.0, 3.6) {K8S\\Cluster};
 
\node[box] (obs) at (3.2, 1.9) {Observability\\Tools};
 
\node[box] (k8sgpt) at (6.0, 0.7) {K8sGPT};
 
\node[box] (rag) at (0, 1.0) {RAG};
 
\node[bigbox] (llmclient) at (0, -1.0) {LLM Client\\z.\,B.\ MCP Client};
 
\node[box] (llm) at (-6.0, -1.0) {Standard LLM};
 
\node[box] (user) at (0, -3.0) {User};
 
% --- Arrows / Beziehungen ---
% User <-> LLM Client
\draw[arrow] (user) -- (llmclient);
 
% LLM Client -> Standard LLM
\draw[arrow] (llmclient) -- (llm);
 
% RAG -> LLM Client (Kontext in Prompt)
\draw[arrow] (rag) -- (llmclient);
 
% Runbooks -> RAG
\draw[arrow] (runbooks) -- (rag);
 
% GitOps -> RAG (Inhalt/Quelle der Wahrheit für Kontext)
\draw[arrow] (gitops) -- (llmclient); % optional: direkter Zugriff/Metadaten
\draw[arrow] (gitops) to[bend left=12] (rag);
 
% Observability -> RAG (Ist-Zustand als Kontext)
\draw[arrow] (obs) -- (rag);
 
% K8s -> Observability (Signals/Telemetry)
\draw[arrow] (k8s) -- (obs);
 
% K8sGPT -> K8s (Cluster Pull / Analyse über API)
\draw[arrow] (k8sgpt) -- node[note, right, xshift=4pt] {Cluster Pull} (k8s);
 
% K8sGPT -> LLM Client (Findings/Diagnose an Assistent)
\draw[arrow] (k8sgpt) -- (llmclient);
 
% LLM Client -> K8sGPT (Analyse anstoßen / Fragen)
\draw[arrow] (llmclient) to[bend left=12] (k8sgpt);
 
% Observability <-> K8sGPT (optional: K8sGPT nutzt/prüft Betriebsdaten)
\draw[arrow] (obs) to[bend left=10] (k8sgpt);
 
% GitOps als Ziel für Änderungen (PR/Commit) – hier bewusst vom User/LLM Client
\draw[arrow] (llmclient) to[bend left=10] node[note, above, yshift=2pt] {PR/Commit} (gitops);
 
% Cluster pullt desired state aus GitOps
\draw[arrow] (k8s) to[bend left=15] node[note, above] {Pull desired state} (gitops);
 
\end{tikzpicture}

